import 'package:flutter/material.dart';

class EmailSignSecond extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Hello'),
    );
  }
}
